import React from "react";
import HomeRoute from "../routes/home";
import Layout from "../layout";

const Home = () => {
  return (
    <Layout>
      <HomeRoute />
    </Layout>
  );
};

export default Home;
